# 🌕 AI 역극 연결 방법 (한 번만 하면 됩니다)

AI 역극이 작동하려면 **두 가지**가 필요해요:

1. **Anthropic API 키** — AI(클로드)를 사용하기 위한 열쇠 (사용한 만큼 요금이 부과됩니다)
2. **Cloudflare Worker** — API 키를 안전하게 숨겨주는 무료 중계 서버

> ⚠️ **왜 이렇게 복잡하게 하나요?**
> API 키를 사이트 코드에 직접 넣으면 누구나 소스보기로 키를 훔쳐가서
> 내 돈으로 AI를 마음껏 쓸 수 있어요. 그래서 키는 반드시 서버에 숨겨야 합니다.

---

## 1부. Anthropic API 키 발급 (약 5분)

1. https://platform.claude.com 에 접속해서 회원가입/로그인
2. 왼쪽 메뉴에서 **API Keys** 클릭
3. **Create Key** 버튼 클릭 → 이름은 아무거나 (예: `dalbit`)
4. 생성된 키(`sk-ant-...`로 시작)를 **복사해서 메모장에 잠시 보관**
   - ⚠️ 이 화면을 닫으면 다시는 볼 수 없어요! 잃어버리면 새로 만들면 됩니다.
5. **Billing(결제)** 메뉴에서 카드를 등록하고 크레딧을 충전
   - 최소 $5부터 충전 가능해요.
   - 역극 대화 1번(주고받기)에 보통 몇 원~몇십 원 수준이라, $5로도 꽤 오래 씁니다.

---

## 2부. Cloudflare Worker 만들기 (약 10분, 무료)

1. https://dash.cloudflare.com 에 접속해서 회원가입/로그인 (무료 플랜이면 충분)
2. 왼쪽 메뉴에서 **Workers & Pages** 클릭
3. **Create(생성)** → **Create Worker** 클릭
4. 이름을 `dalbit-ai` 정도로 바꾸고 **Deploy(배포)** 클릭
5. 배포된 후 **Edit code(코드 편집)** 클릭
6. 편집기에 있는 기존 코드를 **전부 지우고**,
   이 폴더의 **`cloudflare-worker.js`** 파일 내용을 **전체 복사해서 붙여넣기**
7. 오른쪽 위 **Deploy(배포)** 클릭

### API 키 등록하기 (중요!)

8. Worker 화면에서 **Settings(설정)** 탭 클릭
9. **Variables and Secrets** (변수 및 비밀) 항목에서 **Add(추가)** 클릭
   - Type(유형): **Secret** 선택
   - Variable name(변수 이름): `ANTHROPIC_API_KEY` ← 정확히 이렇게!
   - Value(값): 1부에서 복사해둔 `sk-ant-...` 키 붙여넣기
10. **Deploy(저장/배포)** 클릭

### 주소 복사하기

11. Worker 메인 화면에 있는 주소를 복사하세요.
    - 이런 모양이에요: `https://dalbit-ai.내계정이름.workers.dev`

---

## 3부. 사이트에 연결하기 (약 2분)

1. `dalbit-roleplay.html` 파일을 메모장으로 열기
2. 위쪽에서 이 부분을 찾기:
   ```
   const PROXY_URL = '';
   ```
3. 따옴표 안에 2부에서 복사한 주소 붙여넣기:
   ```
   const PROXY_URL = 'https://dalbit-ai.내계정이름.workers.dev';
   ```
4. 저장한 뒤, 평소처럼 GitHub에 파일을 드래그해서 업로드 (덮어쓰기)

---

## ✅ 확인하기

사이트에서 **AI 역극** → 캐릭터 선택 → 프롤로그 끝나고
**"✦ AI와 이야기 이어가기"** 버튼을 눌렀을 때 하루가 대답하면 성공! 🎉

### 안 될 때

| 증상 | 해결 |
|------|------|
| "AI 서버가 아직 연결되지 않았어요" | 3부에서 PROXY_URL 붙여넣기가 안 됐어요. 다시 확인 |
| "잠시 이야기가 끊겼습니다" 반복 | 2부 9번의 변수 이름이 `ANTHROPIC_API_KEY`가 맞는지, 크레딧이 남아있는지 확인 |
| 그래도 안 되면 | 클로드에게 이 파일을 보여주면서 물어보세요 😊 |

---

## 💰 요금 안내

- Cloudflare Worker: **무료** (하루 10만 요청까지)
- Anthropic API: **사용한 만큼만** 충전한 크레딧에서 차감
  - 모델: Claude Sonnet 5 기준, 대화 1회 왕복에 대략 5~30원 수준
  - platform.claude.com의 **Usage** 메뉴에서 사용량 확인 가능
