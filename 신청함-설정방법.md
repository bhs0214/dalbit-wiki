# 🏮 캐릭터 신청함 연결 방법

무림강호에서 쓰던 것과 **똑같은 방식**이에요. (구글 시트 + Apps Script)

## 1. 구글 시트 만들기

1. https://sheets.google.com 에서 새 스프레드시트 생성
2. 이름을 "달빛여관 신청함" 정도로 변경
3. 1행에 제목 입력: `날짜` `이름` `이모지` `구분` `종족` `소속` `나이` `소개` `신청자`

## 2. Apps Script 넣기

1. 시트 상단 메뉴 **확장 프로그램 → Apps Script** 클릭
2. 기존 코드를 지우고 아래 코드를 전체 붙여넣기:

```javascript
function doPost(e) {
  var sheet = SpreadsheetApp.getActiveSpreadsheet().getActiveSheet();
  var data = JSON.parse(e.postData.contents);
  sheet.appendRow([
    data._date || new Date().toLocaleString('ko-KR'),
    data.name || '',
    data.emoji || '',
    data.kind || '',
    data.species || '',
    data.affiliation || '',
    data.age || '',
    data.intro || '',
    data.owner || ''
  ]);
  return ContentService.createTextOutput('ok');
}
```

3. 저장(💾) 클릭

## 3. 웹앱으로 배포

1. 오른쪽 위 **배포 → 새 배포** 클릭
2. 톱니바퀴(⚙️) → **웹 앱** 선택
3. 설정:
   - 실행 계정: **나**
   - 액세스 권한: **모든 사용자** ← 중요!
4. **배포** 클릭 → 권한 허용 (경고가 떠도 "고급 → 이동" 으로 진행)
5. 생성된 **웹 앱 URL** 복사 (`https://script.google.com/macros/s/.../exec`)

> ⚠️ 무림강호 때 배운 것처럼, **나중에 코드를 수정하면
> 반드시 "배포 → 배포 관리 → 새 버전"으로 재배포**해야 반영돼요!

## 4. 사이트에 연결

1. `dalbit-characters.html` 파일을 메모장으로 열기
2. 이 부분을 찾아서:
   ```
   const APPLY_URL = '';
   ```
3. 따옴표 안에 복사한 웹 앱 URL 붙여넣기:
   ```
   const APPLY_URL = 'https://script.google.com/macros/s/..../exec';
   ```
4. 저장 후 GitHub에 업로드

## 5. 신청받은 캐릭터를 인명부에 올리기

1. 구글 시트에서 신청 내용 확인
2. GitHub 웹에서 `characters.json` 파일 열기 → ✏️(연필) 클릭
3. 아래 형식으로 항목 추가 (쉼표 주의!):

```json
[
  {
    "name": "설아",
    "emoji": "🦊",
    "kind": "영물",
    "species": "여우",
    "affiliation": "백호 가문",
    "age": "300살",
    "intro": "눈 내리는 산에서 태어난 여우 영물. 조용하지만 호기심이 많다.",
    "owner": "달빛토끼"
  },
  {
    "name": "두번째캐릭터",
    "emoji": "🐍",
    "kind": "요괴",
    "species": "뱀",
    "affiliation": "무소속",
    "age": "???",
    "intro": "소개글...",
    "owner": "닉네임"
  }
]
```

4. **Commit changes(변경사항 저장)** 클릭하면 사이트에 바로 반영!

> 💡 처음에 들어있는 "예시 캐릭터" 항목은 지우셔도 됩니다.
